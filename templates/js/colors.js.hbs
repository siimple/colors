//Colors list
siimple.colors.list = [ {{#each list as |color|}}'{{ color.name }}'{{#unless @last}}, {{/unless}}{{/each}} ];

{{#each list as |color|}}
//{{capitalize color.name}} palette
siimple.colors.{{ color.name }} = [ {{#each color.values}}'{{this}}'{{#unless @last}}, {{/unless}}{{/each}} ];

{{/each}}

//Index base
siimple.colors.index_base = {{ index_base }};

//Index hover
siimple.colors.index_over = {{ index_over }};
